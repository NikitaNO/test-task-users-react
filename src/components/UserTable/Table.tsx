import * as React from 'react';
// import * as styled from 'styled-components';

const Table: React.StatelessComponent<{}> = (props) => {
  return (
    <div>Table</div>
  )
};

export default Table;
